import { FormErrorFormatterPipe } from './form-error-formatter.pipe';

describe('FormErrorFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormErrorFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
